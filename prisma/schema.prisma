datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}


// Modelo de ejemplo
model User {
  id    Int     @id @default(autoincrement())
  name  String
  email String  @unique
}
